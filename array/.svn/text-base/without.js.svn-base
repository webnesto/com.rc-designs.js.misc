/** @namespace Holds all general utility methods/tools */
var utils = utils || {};

utils.array = utils.array || {};

utils.array.without = function(arr, value) {
	var _arr = arr.slice();
	_arr.splice(_arr.indexOf(value), 1)
	return _arr;
}
