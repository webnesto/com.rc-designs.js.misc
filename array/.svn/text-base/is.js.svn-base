/** @namespace Holds all general utility methods/tools */
var utils = utils || {};

utils.array = utils.array || {};

utils.array.is = function(arr){
	if((!arr) || 
	(arr.constructor!==Array)) {
		return false;
	}
	return true;
}
