/** @namespace Holds all general utility methods/tools */
var utils = utils || {};

utils.array = utils.array || {};

utils.array.has = function(arr, value) {
	//if(!this.is(arr)) return false;
	return (arr.indexOf(value) == -1) ? false : true;
}
